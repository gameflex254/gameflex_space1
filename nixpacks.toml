[phases.setup]
nixPkgs = ["nodejs_22", "bun"]

[phases.install]
cmds = ["bun install --frozen-lockfile"]

[phases.build]
cmds = ["bun run build"]

[start]
cmd = "node dist/server/index.mjs"

[variables]
NODE_ENV = "production"
HOST = "0.0.0.0"
PORT = "8080"